# nothing from this tree is public — it holds his device identifiers and his profile
*.jsonl
*.pcap
.env
data/

# Android build output — the APK is a build product, not a source artifact
android/.gradle/
android/build/
android/app/build/
android/local.properties
