# Live-state receipt — 2026-09-14 06:57 UTC

## Consumed state

`mesh-dash --once adint` showed `step0d-hb-first-cell` active, dashboard marker 14 admits / 208
domains and 239 / 4480 readable pairs, plus unresolved resolver
`unblock/adint/3dd6562eb2e7cc86/resolve`.

## Safe action and dispatch

Retried the resolver's recorded bounded SSH reachability condition. It exited 255 with
`Connection timed out`; therefore `mesh-imac-cam --test` was not run. A read-only
`timeout 25s python3 tools/adint-status` attempt timed out (124), so no fresh corpus derivation
is claimed here.

`mesh-task queue --dispatch --owner 'adint'` exited 0 with no output and no eligible owned row.
No task was checked or taken. No study/frame data or mesh substrate was changed.

## Disposition

The study step remains active and blocked on the camera capability resolver. Retry when bounded
SSH reachability succeeds, then require `mesh-imac-cam --test` to complete a real read before
resuming. The dashboard's domain denominator remains a marker, not a fresh canonical count.
