# Unblock resolve — typed external-event block — 2026-09-16T21:28:36Z

Task: `unblock/adint/a9534782d8636395/resolve` (owner `adint`).

## Live predicates personally checked

- `test -e /run/netns/ru-mobile`: **false**; `ip netns list` showed no `ru-mobile`.
- `100.103.99.16:8022`: **unreachable**; a 5-second TCP probe returned
  `connection refused`.
- The prior retry receipt records Redmi Tailscale `online:false` at 2026-09-16T21:20:56Z.

## Decision

Type: `external-event`.

No safe mesh-owned prerequisite can be implemented from this node: the required approved
RU-mobile namespace/egress and the Redmi SSH service are absent. No routing, VPN, firewall,
capture, or device configuration was changed.

## Exact retry edge

When `/run/netns/ru-mobile` exists with working RU-mobile egress **and**
`100.103.99.16:8022` accepts SSH, rerun the canonical camera/paired capture; verify a fresh
reachable-frame receipt and both-arm vantage, then reconcile the expected 4,480 pairs. Until
both predicates hold, keep `witness-64073-g7-camera-ssh/retry-camera-ssh` typed-blocked and do
not claim the 239/4,480 pilot state is complete.

Delegation: a worker was launched to independently inspect the prior receipt; this decision is
based on the live predicates and this receipt personally inspected by the owning mind.
