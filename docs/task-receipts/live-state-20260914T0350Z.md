# Live adint state — 2026-09-14T03:50Z

`mesh-dash --once adint` showed the active `step0d-hb-first-cell` study on the `ru-mobile`
frame: dashboard marker 14 admits / 208 domains, with 239 / 4480 readable pairs, and the
open resolver `unblock/adint/3dd6562eb2e7cc86/resolve`.

`python3 tools/adint-status` freshly derived 14 canonical admits across 209 touched domains
(3 duplicate rows collapsed) and the same 239 / 4480 readable pairs (5.3%). The 208-domain
dashboard marker is therefore behind the current corpus-derived count; no study rows or
progress markers were changed by the status derivation.

Retried the resolver's documented bounded SSH condition:

```text
timeout 8s ssh -o BatchMode=yes -o ConnectTimeout=5 ilya@192.168.8.214 true
exit 255: ssh: connect to host 192.168.8.214 port 22: Connection timed out
```

The capability condition remains false. Did not attempt `mesh-imac-cam --test`; the resolver
remains blocked until bounded SSH succeeds, then the test must produce a real read. No camera,
mesh service, or substrate state was changed.
