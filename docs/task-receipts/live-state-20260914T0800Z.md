# Live-state receipt — 2026-09-14 08:00 UTC

## Consumed state

`mesh-dash --once adint` reported the active `step0d-hb-first-cell` study, dashboard marker
14 admits / 208 domains and 239 / 4480 readable pairs, plus unresolved resolver
`unblock/adint/3dd6562eb2e7cc86/resolve`.

## Action and dispatch

Retried the resolver's documented bounded reachability condition:

```text
timeout 8s ssh -o BatchMode=yes -o ConnectTimeout=5 ilya@192.168.8.214 true
exit 255: ssh: connect to host 192.168.8.214 port 22: Connection timed out
```

The prerequisite remains unsatisfied, so `mesh-imac-cam --test` was not run. The owned
dispatch query `mesh-task queue --dispatch --owner 'adint'` returned no output and no eligible
row; no task was checked or taken. No camera service, study/frame data, or mesh substrate was
changed.

## Disposition

The study remains blocked on the camera capability resolver. Retry only after bounded SSH
reachability succeeds, then require `mesh-imac-cam --test` to complete a real read before
resuming.
