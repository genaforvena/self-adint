# Self-ADINT study review — 2026-09-07

The safe implementation loop is now present. The measured status artifact is
`data/study-status.json` (private, gitignored), generated by `tools/adint-study`.

## Implemented

- The passive lane records two ordered IP-echo provider attempts per process leg. A dead provider
  is an explicit failed attempt; an all-provider failure leaves the egress unknown.
- `tools/adint-device-capture` performs an explicit, read-only SCP pull of an operator-exported
  PCAPdroid CSV, imports it through the existing parser, and binds the result to a CSV SHA-256
  manifest. Dry-run makes no contact and failed transfers leave no partial artifact.
- `tools/adint-study` reports phase states without network access or irreversible actions. It keeps
  the browser passive corpus separate from the phone corpus and distinguishes `UNKNOWN`,
  `BLOCKED_OPERATOR_EXPORT`, `NOT_ELIGIBLE`, `NO-BASIS`, and operator gates.

## Current measured state

- Step 0 device entry-point artifact: `MEASURED`.
- Step 1 local receiver corpus: `MEASURED`.
- Step 4 web passive lane: `MEASURED`; the append-only ledger contains successful hourly ticks.
- Step 4 device lane: `BLOCKED_OPERATOR_EXPORT`; no operator-exported PCAPdroid CSV has been
  imported in this turn, so no phone claim is made.
- Step 5 active oracle: `NOT_ELIGIBLE`; no measured payload-empty/hashed gate exists.
- Step 6 GAID reset: `NO-BASIS`; no baseline artifact authorises comparing a destructive reset.
- Seat onboarding remains `GATED_OPERATOR_GO` and was not attempted.

## Verification

Passed: `adint-collect-passive --test`, `adint-device-capture --test`, `adint-study --test`,
`adint-import-pcapdroid --test`, `adint-aggregate --test`, `adint-status --test`,
`(cd receiver && go test ./...)`, and Python syntax compilation for the changed tools.

## Exact next action

The operator must export one complete PCAPdroid CSV from his own device and provide its reachable
Termux SSH host, or send the file directly. Then run the documented `adint-device-capture` command;
the next safe turn can produce the device-side `bundle → demand-stack entry point` artifact.
